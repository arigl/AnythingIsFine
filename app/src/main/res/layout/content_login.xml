<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingLeft="16dp"
              android:paddingRight="16dp"
              android:orientation="vertical" >

    <TextView
            android:id="@+id/Label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="What is your name?" android:layout_weight="0" android:paddingTop="0dp"
            android:padding="0dp" android:layout_marginTop="100dp"/>

    <EditText
            android:id="@+id/loginUsernameField"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:inputType="text" android:autofillHints=""
            android:paddingTop="50dp"/>

    <ImageButton
            android:id="@+id/loginButton"
            android:layout_width="60dp"
            android:layout_height="60dp"
            tools:srcCompat="@android:drawable/ic_menu_send" android:paddingBottom="0dp"/>

</LinearLayout>